{
  "name": "@raddus/graph",
  "version": "1.1.0",
  "private": false,
  "description": "Raddus Graph local CLI agent graph app.",
  "type": "module",
  "main": "server/index.mjs",
  "bin": {
    "raddus-graph": "./bin/raddus-graph.mjs",
    "raddus-canvas": "./bin/raddus-graph.mjs"
  },
  "files": [
    "bin",
    "dist",
    "server",
    "server.mjs",
    "package.json"
  ],
  "scripts": {
    "ensure-deps": "[ -x node_modules/.bin/tsc ] && [ -x node_modules/.bin/vite ] || npm ci",
    "dev": "npm run ensure-deps && node bin/raddus-graph.mjs --dev",
    "build": "npm run ensure-deps && tsc --noEmit && vite build",
    "test": "node --test test/*.test.mjs",
    "prepack": "npm run build",
    "publish:patch": "npm ci && npm version patch && npm publish --access public && git push --follow-tags",
    "publish:minor": "npm ci && npm version minor && npm publish --access public && git push --follow-tags",
    "publish:major": "npm ci && npm version major && npm publish --access public && git push --follow-tags",
    "preview": "npm run ensure-deps && node bin/raddus-graph.mjs",
    "start": "npm run ensure-deps && node bin/raddus-graph.mjs"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@vitejs/plugin-react": "^6.0.2",
    "lucide-react": "^1.21.0",
    "react": "^19.2.7",
    "react-dom": "^19.2.7",
    "typescript": "^6.0.3",
    "vite": "^8.0.16"
  },
  "devDependencies": {
    "@types/react": "^19.2.17",
    "@types/react-dom": "^19.2.3"
  }
}
